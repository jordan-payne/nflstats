# Provides a Web Interface for NFL Statistics

Uses [nfldb](nfldb) as its data-source so you need to install the database prior to deploying.

## Installing requirements:
`$make init`

## Testing:
`$make test`

## Running:
`$make run`
